import React from 'react'
import styled from 'styled-components'

export const Footer = () => (
  <StyledFooter>
    <p>Footer</p>
  </StyledFooter>
)

const StyledFooter = styled.footer`
  display: flex;
  margin-top: auto;
`
